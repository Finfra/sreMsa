# Java 설치 — 전 실습 공통 정본

* **이 문서가 Java 설치 절차의 유일한 정본입니다.**
    - lab5(Zipkin)도 이 문서를 정본으로 삼도록 전환 예정입니다. **전환 전까지 lab5 는 자체 절차(openjdk-8)를 그대로 쓰므로**, lab5 만 진행 중이라면 lab5 문서를 따르십시오.
* **Java 가 필요한 곳은 lab5(Zipkin)입니다.** Zipkin 에 로그를 보내는 Spring Boot 예제를 Maven 으로 빌드·실행할 때 씁니다. 설치 노드는 `i1`(실습용 Console서버)입니다.
    - 이 문서가 lab6 폴더에 있는 것은 **설치 절차를 한 곳에만 두기 위해서**입니다. 두 lab 이 각자 절차를 가지면 한쪽만 갱신되어 버전이 갈립니다(실제로 예전에 8 과 11 로 갈려 있었습니다).
    - ⚠️ **lab6(Serverless) 실습 자체에는 Java 가 필요 없습니다.** lab6 의 함수는 Node.js 로 만듭니다. lab6 만 진행한다면 이 문서를 건너뛰십시오.
* 기준 버전은 **OpenJDK 17** 입니다.
    - cf) 예전 자료는 lab5 가 8, 일부 절차가 11 을 쓰도록 갈려 있었습니다. 같은 VM 에서 두 버전을 오가는 혼란을 없애기 위해 **17 하나로 통일**했습니다.

## 1. 설치 [i1에서 실행]

```
sudo apt-get update
sudo apt-get install -y openjdk-17-jdk-headless
```

* 확인 : 아래처럼 나오면 됩니다.
```
java -version
javac -version
```
```
openjdk version "17.0.20" 2026-07-21
OpenJDK Runtime Environment (build 17.0.20+8-1-24.04-Ubuntu)
OpenJDK 64-Bit Server VM (build 17.0.20+8-1-24.04-Ubuntu, mixed mode, sharing)
javac 17.0.20
```

* cf) `-jdk-` 를 깔면 `-jre-` 가 함께 들어옵니다. 실행만 할 거라도 컴파일이 필요한 실습이 뒤에 있으므로 **jdk 로 설치**합니다.

## 2. JAVA_HOME 설정 [i1에서 실행]

```
echo "JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64" | sudo tee -a /etc/environment
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
```

* 위 두 줄은 하는 일이 다릅니다. 첫 줄은 **다음 접속부터** 적용되게 하고, 둘째 줄은 **지금 쓰는 창에** 바로 적용합니다.
* 확인 : 값이 비어 있지 않아야 합니다.
```
echo $JAVA_HOME
```

* **주의 : `~/.bashrc` 에 적는 방식은 쓰지 마십시오.**
    - Ubuntu 의 기본 `~/.bashrc` 는 맨 위에서 **대화형이 아닌 셸을 그냥 빠져나갑니다**(`case $- in *i*) ;; *) return;; esac`).
    - 그래서 사람이 직접 친 명령에서는 값이 보이지만, **`ssh i1 "명령"` 처럼 한 줄로 실행하거나 빌드 스크립트·Ansible 이 실행할 때는 `JAVA_HOME` 이 빈 값**이 됩니다.
    - Maven·Gradle 은 이 값을 보고 JDK 를 찾으므로, 그때 가서 원인 모를 실패가 납니다.
    - `/etc/environment` 는 로그인 과정에서 읽히기 때문에 위 경로 모두에서 값이 잡힙니다.
* cf) `java`·`javac` 실행 파일 자체는 `/usr/bin` 에 등록되므로 `PATH` 를 따로 손댈 필요가 없습니다.
* cf) 설치된 JDK 가 여럿일 때 기본값 확인·변경은 `update-alternatives --display java` / `sudo update-alternatives --config java`

## 3. 컴파일과 실행 확인 [i1에서 실행]

* 실제로 컴파일이 되는지 한 번 확인하고 넘어갑니다.
* **주의 : 파일명과 클래스명이 같아야 합니다.** (`A.java` ↔ `public class A`)

```
cd ~
cat <<EOF> A.java
public class A {
  public static void main(String[] args) {
    System.out.println("hi from " + System.getProperty("java.version"));
  }
}
EOF

javac A.java
ls A.*
java A
```

* 확인 : 아래가 나오면 설치가 끝난 것입니다.
```
hi from 17.0.20
```

* 정리
```
rm -f ~/A.java ~/A.class
```

## 4. 다음 단계

* **lab5(Zipkin)를 진행 중이라면** — 여기까지 하고 lab5 로 돌아가십시오. Maven·Gradle 설치는 lab5 의 `0.java/2.MavenBuild.md`·`3.GradelBuild.md` 에 있습니다.
* **lab6(Serverless)를 진행 중이라면** — 이 문서는 건너뛰어도 됩니다. 곧바로 `1.openfaasInstall` 로 가십시오.
